package com.nanosplit.util;

/** Single source of truth for the version string shown in --version and file headers. */
public final class Version {
    public static final String STRING = "1.0.0";

    private Version() {
    }
}
